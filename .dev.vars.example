# 本地调试：复制成 .dev.vars 再填值（.dev.vars 已在 .gitignore 里，不会被提交）。
# 线上：这个文件也是「Deploy to Cloudflare」按钮的密钥清单——
#       按钮会读它，把下面每一项做成一个输入框让你填，填的值直接存成加密 secret。
#       所以这里只留变量名，不留任何真实值。
#
# 现在只有这一个口令：管理员。
# 组员不用它——他们走管理页发的一次性注册链接，各自换一枚自己的密钥。
ADMIN_PASSWORD= # 管理员口令，用来进 /admin 发注册链接、查名单、撤票
